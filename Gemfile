source "https://rubygems.org"

# Build is handled by our own GitHub Actions workflow (not legacy GitHub
# Pages), so we depend on Jekyll directly rather than the github-pages gem,
# which pins outdated, vulnerable transitive dependencies.
gem "jekyll", "~> 4.4"
gem "jekyll-remote-theme"
gem "webrick"
