# resume.totaldebug.uk

Published output for [https://resume.totaldebug.uk](https://resume.totaldebug.uk).

The rendered site is served by GitHub Pages from the **`gh-pages`** branch of
this repository. Those files are generated automatically — do not edit them
by hand.

The Jekyll source is maintained in a separate private repository and published
here on every change via GitHub Actions.
